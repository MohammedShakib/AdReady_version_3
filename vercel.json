{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm install --prefix server && npm install --prefix client --include=dev && npm run build --prefix client && rm -rf dist && mkdir -p dist && cp -R client/dist/. dist/",
  "outputDirectory": "dist",
  "functions": {
    "api/**/*.js": {
      "maxDuration": 30
    }
  }
}
